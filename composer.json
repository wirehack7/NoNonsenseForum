{
    "name": "nononsenseforum/nononsenseforum",
    "description": "NoNonsense Forum -- dev tooling only (the app itself has no runtime dependencies)",
    "type": "project",
    "license": "CC-BY-3.0",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpstan/phpstan": "^2.2",
        "phpunit/phpunit": "^10.5"
    },
    "config": {
        "sort-packages": true
    }
}
